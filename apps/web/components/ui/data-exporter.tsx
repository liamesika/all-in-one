// Fallback data exporter component
export default function DataExporter() {
  return (
    <div className="p-4 border rounded bg-gray-50 text-gray-600">
      Data Exporter not available - install a data export package
    </div>
  );
}