export const metadata = {
  title: 'E-commerce Dashboard',
  description: 'E-commerce lead management and campaigns',
};

export default function ECommerceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
