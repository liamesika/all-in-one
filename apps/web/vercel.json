{
  "buildCommand": "pnpm run prebuild && pnpm run build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": null,
  "outputDirectory": ".next",
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
