{
  "buildCommand": "cd ../.. && pnpm prisma generate --schema packages/server/db/prisma/schema.prisma && cd apps/web && pnpm run build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
