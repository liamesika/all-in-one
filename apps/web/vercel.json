{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm prisma generate --schema ../../packages/server/db/prisma/schema.prisma && pnpm build",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
