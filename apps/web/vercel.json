{
  "buildCommand": "cd ../.. && pnpm --filter web run prebuild && pnpm --filter web run build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": null,
  "outputDirectory": ".next",
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "true"
    }
  }
}
