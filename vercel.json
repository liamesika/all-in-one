{
  "buildCommand": "npx prisma generate --schema packages/server/db/prisma/schema.prisma && cd apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --force",
  "framework": "nextjs",
  "env": {
    "NEXT_DISABLE_SWC_TOOLS_PATCHING": "1"
  }
}